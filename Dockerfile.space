# Serve pre-built Next.js app to avoid HF Spaces build timeout
FROM node:20-alpine
WORKDIR /app
RUN npm install -g serve
COPY hf_space/.next /app
COPY hf_space/.next/static /app/static 2>/dev/null || true
EXPOSE 3000
CMD ["serve", "-s", "/app", "-l", "3000"]
