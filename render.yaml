# Render Blueprint — OpenLearn AI Service (Docker)
services:
  - type: web
    name: openlearn-api
    env: docker
    region: oregon
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
